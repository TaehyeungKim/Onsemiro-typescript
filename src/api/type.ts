export type SignInData = {
  phone_num: string;
};

export type SignUpPhoneVerifData = {
  phone_num: string;
};

export type SignUpSchoolVerifData = {
  univ: string;
  email: string;
};

export type SignUpCheckVerifData = {
  code: string;
};
